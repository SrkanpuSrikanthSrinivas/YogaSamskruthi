{
  "name": "yogasamskruthi",
  "version": "2.0.0",
  "description": "Full-stack knowledge exchange platform (Gurujis + aspirants) on Vercel + Neon Postgres",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "db:reset": "rm -rf .pgdata && node -e \"require('./src/db').init().then(k=>{console.log('seeded',k);process.exit(0)})\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@electric-sql/pglite": "^0.5.4",
    "@neondatabase/serverless": "^1.1.0",
    "bcryptjs": "^3.0.3",
    "cookie-session": "^2.1.1",
    "ejs": "^6.0.1",
    "express": "^5.2.1",
    "multer": "^2.2.0"
  },
  "engines": {
    "node": "22.x"
  }
}
